# Example Repo

This is a small Python repo for the 12-week agent plan.

- `app.py` is the entry point.
- `config.py` stores app settings.
- `greetings.py` contains the greeting logic.
- `tests/` contains a small pytest suite.

